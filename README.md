# Z-DPV-Life

Static site published with GitHub Pages.

- `index.html` — Zahra's Money Dashboard (self-contained HTML/CSS, no JavaScript)
- `robots.txt` + a `noindex` meta tag keep the page out of search engines. The site is
  still world-readable to anyone with the link.
